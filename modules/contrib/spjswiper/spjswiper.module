<?php

/**
 * Implements hook_page_attachments().
 * @param array $attachments
 */
function swiper_page_attachments(array &$attachments) {
    //add here any conditions if you need to limit the pages

    //$attachments['#attached']['library'][] = 'spjswiper/swiper';
    //$attachments['#attached']['library'][] = 'swiper/init';
}